name: First Workflow
on: workflow_dispatch # manually start the workflow
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello"
      - name: Print goodBye
        run: echo "Done - bye"
